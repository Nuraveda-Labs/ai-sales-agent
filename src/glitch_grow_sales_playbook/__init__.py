"""Calibrated playbook for ai_marketing_stack-sales-agent.

Imports of `RECIPES` from this package override the public stubs at agent
startup. Edit `recipes.py` to tune brand copy; the public engine picks up
the new values on next process start.
"""

__version__ = "0.0.1"
